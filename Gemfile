source "https://rubygems.org"

# Mirrors the gem set GitHub Pages uses to build the site, so local previews
# match production. Run `bundle install` then `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.0+ (webrick was removed from stdlib).
gem "webrick"
